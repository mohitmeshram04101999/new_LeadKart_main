import 'package:flutter/material.dart';


class SeeAllLeads extends StatelessWidget {
  const SeeAllLeads({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

      //
      appBar: AppBar(
        title:const  Text("All Leads"),
      ),

    );
  }
}
